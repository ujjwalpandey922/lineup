import React from 'react';
import './Header.css';

const Header = () => {
  return (
    <header className="sticky-header">
      <h1>My Sticky Header</h1>
    </header>
  );
};

export default Header;
